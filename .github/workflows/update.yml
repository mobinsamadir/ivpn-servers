name: Auto Update Configs

on:
  schedule:
    - cron: '0 */6 * * *' # اجرا هر 6 ساعت یکبار
  workflow_dispatch:      # امکان اجرا دستی دکمه‌ای

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install Dependencies
      run: |
        pip install -r requirements.txt

    - name: Run Collector
      run: |
        python collector.py

    - name: Commit and Push changes
      run: |
        git config --global user.name "GitHub Action"
        git config --global user.email "action@github.com"
        git add servers.txt
        # فقط در صورتی کامیت کن که تغییری ایجاد شده باشد
        git diff --quiet && git diff --staged --quiet || (git commit -m "🔄 Auto-update configs $(date)" && git push)
